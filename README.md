Here are the 3 things a user needs to adjust:

1. Localhost to IP: Change 127.0.0.1 to the PC's actual IP address if using a separate VR headset.

2. Persona: adjust the persona file according to the project.

3. Requirements: A list of Python libraries to install (pip install -r requirements.txt).
